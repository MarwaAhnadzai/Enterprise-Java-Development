CREATE TABLE easy_drinks (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255),
    main1 VARCHAR(255),
    amount1 DOUBLE,
    main2 VARCHAR(255),
    amount2 DOUBLE,
    directions VARCHAR(255)
);
